import java.io.IOException;
import javax.servlet.ServletException;
import javax.servlet.annotation.WebServlet;
import javax.servlet.http.HttpServlet;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;

@WebServlet({"/AddStudentServlet", "/Admin/AddStudentServlet"})
public class AddStudentServlet extends HttpServlet {

    @Override
    protected void doGet(HttpServletRequest request, HttpServletResponse response)
            throws ServletException, IOException {

        String addStudentBtn = request.getParameter("addStudentBtn");
        request.setAttribute("addStudentBtn", addStudentBtn);

        // Redirect to your add student form when you create it.
        response.sendRedirect(request.getContextPath() + "/Admin/Students.html");
    }
}
