import java.io.IOException;
import javax.servlet.ServletException;
import javax.servlet.annotation.WebServlet;
import javax.servlet.http.HttpServlet;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;

@WebServlet({"/AddRoomServlet", "/Admin/AddRoomServlet"})
public class AddRoomServlet extends HttpServlet {

    @Override
    protected void doGet(HttpServletRequest request, HttpServletResponse response)
            throws ServletException, IOException {

        String addRoomBtn = request.getParameter("addRoomBtn");
        request.setAttribute("addRoomBtn", addRoomBtn);

        // Redirect to your add room form when you create it.
        response.sendRedirect(request.getContextPath() + "/Admin/Rooms.html");
    }
}
